import React from 'react';

const Navigation = () => {

    return(
        <div>NavBar
            <button>Cart</button>
            <button>Home</button>
        </div>
    )
};

export default Navigation;